coverage:
  status:
    project:
      default:
        target: 70%
        threshold: 2%
    patch:
      default:
        target: 80%

ignore:
  - "vendor/**/*"
  - "tests/**/*"
  - "build/**/*"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
