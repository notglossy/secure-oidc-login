<?xml version="1.0"?>
<ruleset name="WordPress OIDC Auth Plugin Coding Standards">
	<description>PHPCS configuration for WordPress OIDC Authentication Plugin</description>

	<!-- What to scan -->
	<file>.</file>

	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/dist/*</exclude-pattern>
	<exclude-pattern>*.min.js</exclude-pattern>
	<exclude-pattern>*.min.css</exclude-pattern>
	<exclude-pattern>*/.github/*</exclude-pattern>
	<exclude-pattern>test-types.php</exclude-pattern>

	<!-- How to scan -->
	<arg value="sp"/>
	<arg name="basepath" value="."/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="8"/>

	<!-- Use WordPress-Core standard (strictest, best for PHPCBF) -->
	<rule ref="WordPress-Core">
		<!-- Allow modern short array syntax [] instead of array() -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>

		<!-- Don't require Yoda conditions (modern PHP doesn't need them) -->
		<exclude name="WordPress.PHP.YodaConditions"/>

		<!-- File header docblock conflicts with declare(strict_types=1) in value objects -->
		<exclude name="PSR12.Files.FileHeader.IncorrectOrder"/>

		<!-- Main plugin file doesn't follow class- naming convention (and that's OK) -->
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
	</rule>

	<!-- Minimum WordPress version -->
	<config name="minimum_wp_version" value="5.0"/>

	<!-- PHP version compatibility -->
	<config name="testVersion" value="7.4-"/>
	<rule ref="PHPCompatibilityWP"/>

</ruleset>
